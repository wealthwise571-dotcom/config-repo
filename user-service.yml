server:
  port: 8046
  
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    lease-renewal-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
    
SPRING_DATASOURCE_URL: jdbc:mysql://192.168.1.185:3306/shop_user
