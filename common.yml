spring:
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: {$db_url}
    username: root
    password: customs
    driver-class-name: com.mysql.cj.jdbc.Driver
